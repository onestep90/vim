if has("syntax")
	syntax on
endif

set autoindent "Automatic indentation
set cindent " Automatic indentation for C programming
set hlsearch " Highlight word selected word in find process
set nu " Set line number
set ts=4 " Tab spacing
set shiftwidth=4 " Automatic indentation spacing
set laststatus=2 " Show status bar and set format(include bottom line)
set statusline=\ %<%l:%v\ [%P]%=%a\ %h%m%r\ %F\
set autoread " Automatic reflection of changes that has been occured
set paste " Prevent indentation from breaking when copying and pasting
